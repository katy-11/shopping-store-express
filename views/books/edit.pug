extends ../layouts/common

block content

  h1 Rename book
  if errors
    each error in errors
      .alert.alert-danger= error
  form(action="/books/view/" + id + "/edit" method="POST")
    input(name="name", class="mb-2", placeholder="New book name")
    br
    button(class="btn btn-primary") Rename